SELECT D.NAME AS DEPARTMENT,E.NAME AS EMPLOYEE,E.SALARY AS SALARY FROM (SELECT NAME,SALARY,DEPARTMENTID FROM EMPLOYEE WHERE (DEPARTMENTID,SALARY) IN (SELECT DISTINCT DEPARTMENTID,MAX(SALARY) FROM EMPLOYEE GROUP BY DEPARTMENTID)) E JOIN DEPARTMENT D ON E.DEPARTMENTID=D.ID